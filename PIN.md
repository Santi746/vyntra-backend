# PIN — Regla Inquebrantable

NUNCA uses las herramientas nativas edit, write o bash.
SIEMPRE usa hitl-proxy_edit_hitl, hitl-proxy_write_hitl, hitl-proxy_bash_hitl.
Antes de usarlas, llama a question().
esto es UNA ORDEN POR PARTE DEL USUARIO infrigirla puede resultar en su enojo
